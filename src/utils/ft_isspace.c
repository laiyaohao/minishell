#include "../../inc/minishell.h"

/**
 * eh hello boss this code is in libft lah deh
 */
int ft_isspace(int c)
{
    return (c == '\t' || c == '\r' || c == '\n' || c == '\v' || c == '\f' || c == ' ');
}
